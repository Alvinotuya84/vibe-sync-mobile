import { View, Text } from "react-native";
import React from "react";
import { Stack } from "expo-router";

type Props = {};

const GigsLayout = (props: Props) => {
  return <Stack />;
};

export default GigsLayout;

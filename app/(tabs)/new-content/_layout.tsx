import { View, Text } from "react-native";
import React from "react";
import { Stack } from "expo-router";

type Props = {};

const NewContentLayout = (props: Props) => {
  return <Stack />;
};

export default NewContentLayout;
